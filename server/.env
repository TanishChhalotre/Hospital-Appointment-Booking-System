# ── MongoDB 
MONGODB_URI=mongodb+srv://tanishchhalotreemail_db_user:tanish123@cluster0.ppglc4r.mongodb.net/?appName=Cluster0

# ── JWT 
# Must be at least 32 characters. Generate one with: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
JWT_SECRET=tanishchhalotre123tanishchhalotre

# ── Server 
PORT=5000
CLIENT_URL=http://localhost:5173

# ── Seed 
SEED_DATABASE=true

# ── Email (OTP) 
# For Gmail: enable 2FA on your account, then create an App Password at
# https://myaccount.google.com/apppasswords
# Use that 16-character App Password as SMTP_PASS (not your real Gmail password)
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=tanishchhalotrespam@gmail.com
SMTP_PASS=yulc hojs lywa nsuu
